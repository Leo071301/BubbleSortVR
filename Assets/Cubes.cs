using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class Cubes : MonoBehaviour
{

    public static void createCubeList(List<int> list_in, Material material, Color color_in)
    {
        List<GameObject> cube_array = new List<GameObject>(); // initialize the list of cubes variable

        for (int i = 0; i< list_in.Count; i++)
        {
            // 
        }

    }

    // insertion sort
}
